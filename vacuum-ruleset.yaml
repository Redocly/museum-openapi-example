extends: [[spectral:oas, recommended]]

rules:
  my-vacuum-rule:
    description: Tags must have a description.
    given: $.tags[*]
    severity: error
    then:
      field: description
      function: truthy